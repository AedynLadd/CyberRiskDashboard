### APPLICATION CODE
application code
core: 
    all singleton services and any code that needs to be shared throughout the application.
modules: 
    all major application modules.
shared: 
    shared components, directives, pipes etc. that needs to be re-used and referenced by the components declared in other feature modules.